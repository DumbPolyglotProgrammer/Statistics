{
  "name": "statistics",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "clean": "rm -rf node_modules build",
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "devDependencies": {
    "@types/node": "^14.10.1",
    "concurrently": "^5.3.0",
    "husky": "^4.2.5",
    "nodemon": "^2.0.4",
    "prettier": "^2.0.5",
    "pretty-quick": "^3.0.0"
  }
}
