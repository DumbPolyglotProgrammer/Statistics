{
  "name": "statistics",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "clean": "rm -rf node_modules build",
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.4"
  }
}
